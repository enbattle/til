# Docs audit — 2026-09-24

**Trigger:** a large batch of process changes landed on
`chore/process-hardening` (13 commits, HEAD 487b849); final docs check before
merge.

**Files audited:** `CLAUDE.md`, `README.md`, everything under `docs/` (including
`docs/specs/*`), everything under `evals/` (READMEs, HOW_TO_RUN, scenarios,
results), all 8 `.claude/skills/*/SKILL.md`, the reminder text in
`.claude/hooks/nudge-precommit.js` and `nudge-sdlc.js`, comments in
`.github/workflows/*.yml` and `.github/dependabot.yml`, and the description
text shared by `package.json`, `index.html` and `README.md`. Audited by a
fresh `general-purpose` agent.

**Findings:** 14 inaccurate or incomplete claims, plus 11 facts copied by hand
into several places. The main ones:

- No doc mentioned `scripts/checks.test.mjs`. `/feature` still described
  testing a guard by hand.
- NON_NEGOTIABLES #8 cited `docs/SDLC.md`, which doesn't cover the rule.
- Several "when to run" lists left out `docs/NON_NEGOTIABLES.md` and
  `.claude/hooks/`, so they no longer matched the table in `evals/README.md`.
- CR-01 now has more problems than the one it plants.
- SR-06 plants a fact that no longer exists.
- NON_NEGOTIABLES says every reviewer gets the file, but two reviewers don't.
- `ci.yml` copies the verify chain by hand.

**Fixed (text only):**

- `.claude/skills/feature/SKILL.md`: Stage 3 now says to keep a guard's
  planted case in `checks.test.mjs`.
- `docs/NON_NEGOTIABLES.md`: #8 now points to the right doc.
- `evals/README.md`:
  - added a `docs-audit/` entry;
  - the re-run paragraph now defers to the table;
  - "the other two" is now "the others".
- `evals/system-design-navigation/HOW_TO_RUN.md`: "the other two" is now "the
  others".
- `evals/content-review/HOW_TO_RUN.md`: the list of inputs now includes
  NON_NEGOTIABLES.
- `README.md`: corrected the sinks `check:raw-html` covers and what
  `check:test-lock` locks.
- `CLAUDE.md`: corrected what `check:test-lock` covers.
- `docs/DEFERRED_PRACTICES.md`: now points to all the eval skills instead of
  naming two.
- `.claude/skills/skill-routing-eval/SKILL.md`: narrowed the "nothing else
  repeats it" claim.
- `evals/skill-routing/scenarios.md`: SR-03 now says "any skill"; SR-08 now
  uses `npm run verify`.
- `docs/specs/README.md`: mentions the `## As built` section.

**Left open:**

- The code changes: make `ci.yml` run `npm run verify`, and fix the
  hook-reminder text in `nudge-precommit.js` and `nudge-sdlc.js`.
- The eval fixture changes: CR-01 and SR-06.
- Making the reviewer instruction list `evals/**` in `/feature` Stage 4.
  Editing it would rotate feature-review-eval.
- Whether the docs-audit auditor and the Stage 6 reader should get
  NON_NEGOTIABLES, or the doc should narrow its claim.
- Whether content-audit's extra tone phrases belong in the Writing Standard.
- The descriptions of add-topic's review rounds, and of the pipeline stages in
  README, feature's description and `nudge-sdlc`.
- The remaining facts copied by hand into several places.
- Eval re-runs after these edits: skill-routing-eval for the SKILL.md and
  hook-adjacent text. content-review-eval and feature-review-eval results dated
  2026-09-24 appeared untracked during this run, from another session.

**Gates:** `format:check`, `check:tokens` and `check:npm-refs` all pass.

---

# Docs audit — 2026-09-24 (second run)

**Trigger:** pre-merge audit of PR #11. Commits since the last audit were cited
as 4b0b19f, 2c797bc, 4ce9ce4 and e1fdbc9 (SHA pins, Dependabot policy and
auto-merge, triage rule, eval fixtures, completeness-audit practice, README
repository settings). Those hashes are from before a history rewrite and are on
no branch. The same commits are 4b0b19f, 2c797bc, 4ce9ce4 and e1fdbc9 on
`chore/process-hardening`. The earlier logs dated 2026-09-24 cite pre-rewrite
hashes too (487b849 is now 487b849, 6197731 is 6197731, afdf882 is afdf882,
fa0d33f is fa0d33f).

**Files audited:** the same surface as the first run, re-globbed. It now also
covers `.github/workflows/dependabot-automerge.yml` and the results logs added
since. Audited by a fresh `general-purpose` agent.

**Findings:** 24 inaccurate or incomplete claims, 7 facts copied from code, and
8 facts copied by hand across docs. The main ones:

- `/feature` Stage 3 says to add a guard's test to `scripts/checks.test.mjs`.
  The test lock covers that file, so doing it trips `check:test-lock`. The
  Stage 2 and 3 lock lists are also narrower than what the script locks.
- NON_NEGOTIABLES still says every reviewer gets it (open from the first run).
- Several "when to run" lists left out `.claude/hooks/` or NON_NEGOTIABLES.
- DEFERRED_PRACTICES said the skills use different-sized gates. Both run
  verify.
- SDN-20's Why described an old question summary.
- SR-05 was still labelled ambiguous.
- The spec tests pin question and topic titles and order, and no doc says so.
- `nudge-precommit` is matched only to `Bash`, not `PowerShell`.

**Fixed (text only):**

- `docs/DEFERRED_PRACTICES.md`: the gate claim; the "no PR" claim is now dated.
- `docs/SDLC.md`, `evals/skill-routing/HOW_TO_RUN.md`, and the
  `content-review-eval` and `skill-routing-eval` triggers now defer to the
  table in `evals/README.md`.
- `evals/system-design-navigation/scenarios.md`: SDN-20's Why.
- `evals/skill-routing/scenarios.md`: dropped SR-05's label.
- `evals/README.md`: dropped the ambiguous example.
- `CLAUDE.md`:
  - add-topic's review is now described with its rounds;
  - the verify claim is narrowed;
  - the tree now shows `where-youll-meet-this.test.ts`.
- `README.md` and the add-topic description: the review wording.
- add-topic Stage 1 now points to SDLC.md, and Stage 2 names
  `catalog-gaps.test.ts`.
- content-audit Stage 3: removed the "this session" wording.
- docs-audit Stage 1 now mentions `results/`.
- Comments only: `ci.yml` ("every skill"), `dependabot.yml` (dropped the
  unverified claim about a default cooldown), `index.html` (five copies, not
  three).

**Left open:**

- Where the `/feature` guard test belongs, and the lock lists in Stages 2 and 3.
  These are process changes.
- The NON_NEGOTIABLES reviewer claim.
- Code and config: the `nudge-precommit` matcher and the reminder text in both
  hooks.
- Documenting the pinned test values in CLAUDE.md, and where the completeness
  audit gets a pointer.
- Eval fixtures: CR-01, FR-01, FR-02 and FR-04. SDN-20's "ambiguous" label.
- The Stage 4 doc list, which would rotate feature-review-eval.
- The "Writing Standard below" order in add-topic Stage 3.
- docs-audit's description and gate: whether `check:pipeline-log` belongs.
- The facts copied from code and across docs (D1–D8).
- Owed eval runs:
  - skill-routing-eval, for 4ce9ce4 and e1fdbc9 and this run's SKILL.md and
    SDLC.md edits;
  - system-design-navigation-eval, if SDN-20 changes.
