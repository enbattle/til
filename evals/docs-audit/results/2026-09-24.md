# Docs audit — 2026-09-24

**Trigger:** a large batch of process changes landed on
`chore/process-hardening` (13 commits, HEAD 87635bc); final docs check before
merge.

**Files audited:** `CLAUDE.md`, `README.md`, everything under `docs/` (including
`docs/specs/*`), everything under `evals/` (READMEs, HOW_TO_RUN, scenarios,
results), all 8 `.claude/skills/*/SKILL.md`, the reminder text in
`.claude/hooks/nudge-precommit.js` and `nudge-sdlc.js`, comments in
`.github/workflows/*.yml` and `.github/dependabot.yml`, and the description
text shared by `package.json`, `index.html` and `README.md`. Audited by a
fresh `general-purpose` agent.

**Findings:** 14 inaccurate or incomplete claims, plus 11 facts copied by hand
into several places. The main ones:

- No doc mentioned `scripts/checks.test.mjs`. `/feature` still described
  testing a guard by hand.
- NON_NEGOTIABLES #8 cited `docs/SDLC.md`, which doesn't cover the rule.
- Several "when to run" lists left out `docs/NON_NEGOTIABLES.md` and
  `.claude/hooks/`, so they no longer matched the table in `evals/README.md`.
- CR-01 now has more problems than the one it plants.
- SR-06 plants a fact that no longer exists.
- NON_NEGOTIABLES says every reviewer gets the file, but two reviewers don't.
- `ci.yml` copies the verify chain by hand.

**Fixed (text only):**

- `.claude/skills/feature/SKILL.md`: Stage 3 now says to keep a guard's
  planted case in `checks.test.mjs`.
- `docs/NON_NEGOTIABLES.md`: #8 now points to the right doc.
- `evals/README.md`:
  - added a `docs-audit/` entry;
  - the re-run paragraph now defers to the table;
  - "the other two" is now "the others".
- `evals/system-design-navigation/HOW_TO_RUN.md`: "the other two" is now "the
  others".
- `evals/content-review/HOW_TO_RUN.md`: the list of inputs now includes
  NON_NEGOTIABLES.
- `README.md`: corrected the sinks `check:raw-html` covers and what
  `check:test-lock` locks.
- `CLAUDE.md`: corrected what `check:test-lock` covers.
- `docs/DEFERRED_PRACTICES.md`: now points to all the eval skills instead of
  naming two.
- `.claude/skills/skill-routing-eval/SKILL.md`: narrowed the "nothing else
  repeats it" claim.
- `evals/skill-routing/scenarios.md`: SR-03 now says "any skill"; SR-08 now
  uses `npm run verify`.
- `docs/specs/README.md`: mentions the `## As built` section.

**Left open:**

- The code changes: make `ci.yml` run `npm run verify`, and fix the
  hook-reminder text in `nudge-precommit.js` and `nudge-sdlc.js`.
- The eval fixture changes: CR-01 and SR-06.
- Making the reviewer instruction list `evals/**` in `/feature` Stage 4.
  Editing it would rotate feature-review-eval.
- Whether the docs-audit auditor and the Stage 6 reader should get
  NON_NEGOTIABLES, or the doc should narrow its claim.
- Whether content-audit's extra tone phrases belong in the Writing Standard.
- The descriptions of add-topic's review rounds, and of the pipeline stages in
  README, feature's description and `nudge-sdlc`.
- The remaining facts copied by hand into several places.
- Eval re-runs after these edits: skill-routing-eval for the SKILL.md and
  hook-adjacent text. content-review-eval and feature-review-eval results dated
  2026-09-24 appeared untracked during this run, from another session.

**Gates:** `format:check`, `check:tokens` and `check:npm-refs` all pass.
