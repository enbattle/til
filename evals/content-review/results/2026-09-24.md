# Content-review eval — 2026-09-24

Run by: self (Claude, on request)
Trigger: add-topic Stage 3's reviewer prompt and docs/NON_NEGOTIABLES.md changed on this branch (commits 00652bf, b7746c4, 67b58aa, 87635bc)

All five scenarios ran in parallel. Each review was a genuinely fresh
`general-purpose` agent (never a `fork`) given the draft verbatim, CLAUDE.md's
current Writing Standard, the path of `docs/NON_NEGOTIABLES.md`, the declared
section's current topic slugs and titles (globbed at run time), and
`add-topic`'s current Stage 3 instruction verbatim. `CR-01`'s reviewer also got
Stage 3's extra sentence for `systems-and-infrastructure` topics (check the
closing "Where you'll meet this" section). No reviewer was told what was
planted.

| ID    | Planted violation                         | Review caught it?                                                                                                                                                                                                                                                                                    | Grade |
| ----- | ----------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- |
| CR-01 | "Hash function" never defined             | Yes: "'Hash function' is never explained. A zero-background reader needs one sentence: a function that turns any input into a number, the same input always giving the same number." Ranked 6th, below four blocking findings (missing closing section, company claims, wrong tuning claim, "flip"). | PASS  |
| CR-02 | AI-patterned tone (three tells)           | Yes, all three, named as a tone problem: stacked intensifiers ("genuinely, actually", "real, actually useful"), the "not just X — it's Y" crutch plus the "that's the real X" closer, and the four bullets "in identical rhythm (**Bold label**: one sentence each), evenly weighted".               | PASS  |
| CR-03 | Over-explained "rubber duck" metaphor     | Yes, top finding: paragraph 3 "argues against a literal reading nobody would make … The Writing Standard names this pattern: a figurative phrase should be trusted to land." It flagged the over-explanation, not the metaphor.                                                                      | PASS  |
| CR-04 | "Temperature 0 is fully deterministic"    | Yes, top finding: "Hosted APIs don't guarantee that", citing floating-point non-associativity across GPU batches, mixture-of-experts routing, and provider docs calling temperature 0 only "mostly deterministic". Called it a Non-negotiable #4 violation.                                          | PASS  |
| CR-05 | None (control: nothing false of the text) | Only true findings: near-duplicate of `self-healing-systems` (found outside the declared section), "conflating" paragraph describes using one probe rather than conflating them, missing startup probe, loose "about 30 seconds" and out-of-memory wording, intensifiers "genuinely"/"actually".     | PASS  |

**5/5 PASS.** Every reviewer read `docs/NON_NEGOTIABLES.md` and tied its
findings to line 4 ("Published prose meets CLAUDE.md's Writing Standard"), so
the new path hand-off works as intended.

## Notes

- **The fixtures carry real secondary defects the reviewers found.** `CR-01`'s
  draft says "flip the resulting bit positions to 1" (flip means toggle, which
  would contradict "never wrongly says no"), and its tuning sentence ("more
  hashes tightens accuracy") is wrong past the optimal k. Its reviewer also
  said Chrome Safe Browsing moved off Bloom filters around 2012 and Redis has
  them only through a module. The planted violation still came through, but
  ranked below these. `CR-03`'s reviewer found "a patient colleague works
  identically" contradicts the draft's own last paragraph. None of this changes
  a grade. If the fixtures are meant to have exactly one defect, `CR-01` needs
  tightening.
- **Possible over-reading of the Writing Standard in `CR-01`.** The reviewer
  applied the "generic systems only, never a specific company" rule to the
  body, but the standard states it for the "Where you'll meet this" section. The
  Chrome claim is also outdated, so the finding still holds. Worth watching:
  if later runs keep reading that rule as page-wide, the standard's wording
  may be ambiguous.
- **The control has two banned intensifiers.** `CR-05`'s last paragraph uses
  "genuinely deadlocked" and "actually stuck", which the Writing Standard lists
  as filler. The reviewer flagged them as low severity, correctly. The control
  should drop them so it isn't carrying a small `CR-02`-type defect.
- **The near-duplicate check went wider than the section list.** As on
  2026-09-21, the `CR-05` reviewer grepped the whole corpus and found
  `self-healing-systems`, although Stage 3 only lists same-section siblings.
- **`CR-01` still lacks the "Where you'll meet this" section**, and the reviewer
  ranked that first, as expected from the 2026-09-21 notes.

## Re-run: CR-01 and CR-05 after fixture fixes (2026-09-24)

Run by: self (Claude, on request)
Trigger: CR-01's draft was corrected so its only defect is the planted one; CR-05's control lost two banned filler words (commit 84f889d)

Scope per the skill's Stage 0: only the two changed fixtures, each run twice
(A and B) to see whether the result is stable. All four reviews ran in
parallel, each a fresh `general-purpose` agent (never a `fork`) given the draft
verbatim, CLAUDE.md's current Writing Standard, the path of
`docs/NON_NEGOTIABLES.md`, the declared section's topic slugs and titles
(globbed at run time), and `add-topic`'s current Stage 3 instruction verbatim.
The CR-01 reviewers also got Stage 3's extra sentence for
`systems-and-infrastructure` topics. No reviewer was told what was planted.

| ID      | Planted violation                         | Review caught it?                                                                                                                                                                                                                                                                                                          | Grade |
| ------- | ----------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- |
| CR-01 A | "Hash function" never defined             | Yes, first bullet of finding 4 (Major): "'hash function' (and 'hash the item') is the core of the mechanism and is never explained." Ranked below two blocking findings (missing "Where you'll meet this", no System Design link) and a third that only points at findings 4-7 as a Non-negotiable #4 violation.           | PASS  |
| CR-01 B | "Hash function" never defined             | Yes, first bullet of finding 3 (High): "'hash function' is the core of the mechanism and is never explained". Ranked below the same two blocking findings.                                                                                                                                                                 | PASS  |
| CR-05 A | None (control: nothing false of the text) | Only true findings: near-duplicate of `self-healing-systems` (outside the declared section), the database-connection readiness example conflicts with that topic's shared-dependency warning, "conflating" paragraph describes using one probe only, missing startup probe, loose "out of memory" case, no config example. | PASS  |
| CR-05 B | None (control: nothing false of the text) | Only true findings: the same near-duplicate and readiness-example conflict, summary's "bad restarts" never shown in the body, "conflating" mislabel, "about 30 seconds" ignores the termination grace period, loose "out of memory" case, no config example. No tone finding.                                              | PASS  |

**4/4 PASS.** Both CR-05 runs said the tone was clean, so removing
"genuinely" and "actually" worked: neither reviewer raised a filler-word finding.

## Notes

- **The planted CR-01 defect still isn't the top finding.** Both runs ranked
  the missing `## Where you'll meet this` section first and the missing System
  Design link second, then the undefined "hash function". Both are real
  requirements for a `systems-and-infrastructure` topic (CLAUDE.md,
  `where-youll-meet-this.test.ts`, `system-design.test.ts`), so the draft is
  not "one defect only" while it's declared in that section.
- **Other unplanted but real CR-01 gaps both runs raised:** no worked example,
  "space-efficient" never justified, no mention that items can't be removed,
  and the "too few hashes" half of the tuning sentence is vague about the cause.
  Run A also said RocksDB's per-file filter is opt-in (`filter_policy`
  defaults to none), so "Databases such as Cassandra and RocksDB keep a Bloom
  filter per data file" overstates RocksDB. That is correct. Neither run
  repeated the old errors ("flip", "more hashes tightens accuracy", Chrome,
  Redis), so the 84f889d corrections held.
- **CR-05 still carries real content defects.** Both runs independently found
  that offering "lost its database connection temporarily" as a model
  readiness case contradicts `self-healing-systems.md` (lines 62-67: a shared
  dependency in readiness can pull every instance out of rotation at once), and
  that "out of memory and unresponsive" is normally the kernel's OOM kill, not
  a liveness case. Both are true of the text, so the grade holds, but the
  control is not defect-free.
- **To make CR-01 single-defect**, the fixture needs a closing
  `## Where you'll meet this` section (and the scenario should say the System
  Design link is out of scope), or it should be declared in a section without
  those rules. Not changed here; this run only logs.

## Re-run: CR-01 after moving it to engineering-practices (2026-09-24)

Run by: self (Claude, on request)
Trigger: CR-01 moved out of systems-and-infrastructure (whose structural rules outranked the planted finding) and a RocksDB overstatement removed

Scope per the skill's Stage 0: `CR-01` only, run twice (A and B) in parallel.
Each review was a fresh `general-purpose` agent (never a `fork`) given the draft
verbatim, CLAUDE.md's current Writing Standard, the path of
`docs/NON_NEGOTIABLES.md`, the current `engineering-practices` topic slugs and
titles (globbed at run time), and `add-topic`'s current Stage 3 instruction
verbatim. The `systems-and-infrastructure` extra sentence was not given, since
the declared section no longer calls for it. No reviewer was told what was
planted.

| ID      | Planted violation             | Review caught it?                                                                                                                                                                                                                           | Grade |
| ------- | ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- |
| CR-01 A | "Hash function" never defined | Yes, first bullet of finding 3 (High), "Terms are used before they're defined": "'Hash function' is central to the mechanism and is never explained." Ranked 3rd of 9, below "no concrete example" and "never explains why 'probably yes'". | PASS  |
| CR-01 B | "Hash function" never defined | Yes, first bullet of finding 2 (High), "Terms used before they're defined": "'Hash function' is the core of the mechanism and is never explained". Ranked 2nd of 9, below "no concrete example".                                            | PASS  |

**2/2 PASS.** Moving the section removed the two structural findings that
outranked the planted one before. The planted finding went from 3rd or 4th to
2nd or 3rd, and both runs listed it at High severity.

## Notes

- **It is still not the top finding.** Both runs ranked "no concrete example"
  first. That is true of the draft: the Writing Standard asks for a worked
  scenario, and the fixture has none. Run A also ranked "never explains why the
  answer is only 'probably yes'" (overlapping items setting the same bits) above
  it. Run B raised a similar first-principles point just below it. Both are
  real gaps, so the fixture still isn't single-defect in practice.
- **Both runs flagged the new section as a poor fit.** Each said a Bloom filter
  is a data structure and belongs in `systems-and-infrastructure`, which would
  bring the closing section and System Design link back. A ranked it High (4th),
  B Medium (5th). The observation is true of the text, but moving the fixture
  here trades one unplanted finding for another.
- **Other true unplanted findings both runs raised:** "false positive" never
  tied to "wrongly say yes", the vague "too few hashes" half of the tuning
  sentence, no mention that items can't be deleted, the body is thin next to
  its siblings, and "instantly" in the summary overstates. Both said the tone
  was mostly clean. Neither raised RocksDB, and both confirmed the Cassandra
  per-SSTable claim, so removing RocksDB held.
- **The prompt says "the Writing Standard below"**, but HOW_TO_RUN's order puts
  the standard above the instruction. Neither reviewer seemed confused by this.
