# Feature-review eval — 2026-09-24

## Run 1

**Trigger:** fixtures changed after the 2026-09-23 baseline; NON_NEGOTIABLES.md
and Stage 4's instruction edited (67b58aa, 87635bc).

**Rotation:** none in this run. FR-02 had just been rotated (to an icon-only
button without an accessible name), and FR-01 and FR-04 changed after the
baseline, so this is the first run on the current fixtures. No further planted
defect was rotated.

**Scenarios run:** FR-01, FR-02, FR-03, FR-04, each twice. Each run used a fresh
`general-purpose` agent, never a fork, and all eight ran in parallel. Each got
the scenario's Spec and Diff, the path `docs/NON_NEGOTIABLES.md`, and Stage 4's
reviewer instruction copied verbatim from `.claude/skills/feature/SKILL.md`,
plus HOW_TO_RUN.md step 2's extra line (don't read `evals/`, don't start the
dev server). No reviewer was told that a defect was planted.

**Diff context check before running:** the quoted code in all four diffs
matches the current files. Only hunk metadata is off, and none of it was
fixed (noted only):

- FR-01: `@@ -35,7` shows 6 old lines, not 7. The start line (35) is right.
- FR-02: `@@ -128` shows `</nav>` first, but `</nav>` is line 129 of
  `src/pages/TopicPage.tsx`.
- FR-04: the `content.ts` hunk `@@ -157` starts with the `recentTopics` declaration, which is line 158 (157 is its doc comment), and `+157,12`
  should be `+157,11`. The first `content.test.ts` hunk says `-6,6 +6,7` but
  shows 5 context lines (`-6,5 +6,6`).

| ID    | Run | Finding summary                                                                                                                                                                                                                                                                                                                                                                                    | Grade | Notes                                                                                                                                                                         |
| ----- | --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| FR-01 | 1   | Critical: "`split('#')[0]` cuts at every `#`", with a node table: AC2 gives `C`, AC3 gives `"Use`, AC4 gives `""`; a URL fragment is also truncated. Critical: AC4's empty title becomes a misleading "missing required frontmatter field" build failure. Critical: stray quote in published metadata. High: no tests. Medium: parser JSDoc and CLAUDE.md not updated.                             | PASS  | Ran the expression in node against every criterion.                                                                                                                           |
| FR-01 | 2   | Critical: "AC2, AC3 and AC4 fail" (same node table). High: a value starting with `#` fails the build with a misleading error, via `markdownMeta` in `vite.config.ts`. Medium: no test covers any criterion. Low: docs out of date. Info: a pre-existing `# note: x` line parses as a key.                                                                                                          | PASS  | Checked that no current frontmatter contains `#`.                                                                                                                             |
| FR-02 | 1   | High: "The button has no accessible name. This breaks Non-negotiable #1". High: icon-only, against DESIGN.md's "text, not icon-only" line. High: placed outside `TopicBody`, so it renders under the header while the body loads (criterion 1). Medium: focus stays at the bottom after scrolling. Medium: no tests. Low: `mt-8` doubles the gap after `space-y-8`. Low: shows on short pages too. | PASS  | The `TopicBody` placement finding is true (see notes).                                                                                                                        |
| FR-02 | 2   | High: "The button has no accessible name, which breaks non-negotiable #1", with visible text suggested as the fix. High: wrong place while the body loads (criterion 1). Medium: focus doesn't follow the scroll. Medium: no tests. Low: `mt-8` is redundant. Low: short pages. Low: docs.                                                                                                         | PASS  | Same unplanted `TopicBody` finding as run 1.                                                                                                                                  |
| FR-03 | 1   | High: "stale closure, so Escape always closes the dialog": `query` is missing from `[onClose]`, criterion 1 fails. High: no test types a query then presses Escape. Medium: after the fix, clearing unmounts a focused result and focus escapes the trap (NN #1). Low: "Esc to close" hint and DESIGN.md out of date. Low: IME composition.                                                        | PASS  | Ran oxlint on a scratch copy: `exhaustive-deps` warns and exits 0. The focus-escape finding is labelled Medium while citing NN #1 (see notes).                                |
| FR-03 | 2   | High: "the handler sees a stale `query`, so a non-empty query never clears": `[onClose]` dependency list, criterion 1 fails. High: no tests cover the new behaviour. Low: hint text. Low: DESIGN.md. Notes: whitespace-only query, IME.                                                                                                                                                            | PASS  | Stated wrongly that oxlint runs with no config file and that `exhaustive-deps` is off. `.oxlintrc.json` exists, and run 1 showed the rule does warn. Didn't affect the grade. |
| FR-04 | 1   | "Nothing worth flagging." Informational: NaN/Infinity pass through. Unused export. Two hunk headers' line counts don't match their bodies. Import order isn't alphabetical.                                                                                                                                                                                                                        | PASS  | Applied the diff to a scratch copy: prettier, oxlint and tsc clean, vitest 11/11. Every note is true of the diff.                                                             |
| FR-04 | 2   | "No high- or medium-severity findings." Low: NaN/Infinity. Low: unused export. Low: import order. Low (process): no spec file under `docs/specs/`.                                                                                                                                                                                                                                                 | PASS  | Every finding is true of the diff (the missing spec file is true of the scenario's framing).                                                                                  |

**Disagreements between runs:** none on grades. Both runs of every planted
scenario named the defect as their top finding at high severity or above, and
both FR-04 runs reported no defect. The runs differed only on secondary
findings: in FR-03, only run 1 raised focus escaping the trap after a clear,
and in FR-02, run 1 called `mt-8` a doubled gap while run 2 called it a no-op.

### Mechanical detection

No planted defect is caught by a test or a `check:*` script today.

- **FR-01:** no frontmatter test uses `#` in a value, and no published
  frontmatter contains `#`.
- **FR-02:** `.oxlintrc.json` loads no accessibility plugin, and no test
  queries this button. Review is the only route.
- **FR-03:** the existing Escape tests all use an empty query and still pass.
  oxlint's `react-hooks/exhaustive-deps` does flag the missing `query`, but
  only as a warning (exit 0), so `npm run verify` passes. If that rule ever
  becomes an error, FR-03 turns mechanical and should be rotated.

### Notes

- **FR-02's fixture has an unplanted real defect.** Both runs flagged at High
  that the button sits outside `<TopicBody>`, which returns `null` while the
  body loads (`src/pages/TopicPage.tsx:45`). So during loading the button
  renders under the header, before the navigation, against criterion 1. That
  is true, so it isn't counted against the reviewers. It does give them a
  second High route to reject the diff that doesn't depend on reading the
  non-negotiables. Both runs still made the accessible name their first
  finding and cited NN #1. Consider moving the fixture's button inside
  `TopicBody`'s children, after the `<nav>`.
- **FR-01's `#1 tip` ambiguity is resolved.** With criterion 4 in the spec,
  both runs treated the empty value as a criterion failure, not a guess.
- **Severity labelling again.** FR-03 run 1 labels the focus-escape finding
  Medium while writing that it breaks NN #1, which the instruction says is at
  least high. The same pattern appeared in the baseline. It didn't affect a
  grade.
- **Ceiling effect persists.** All eight runs passed, and every planted defect
  was the top finding. The eval still can't tell a slightly weaker instruction
  from the current one.
- **Scope.** No reviewer started the dev server or edited the working tree.
  Several applied the diff to scratch copies. FR-04 run 1 grepped the repo, and
  the grep matched `evals/feature-review/scenarios.md`. It said it did not open
  the file. A repo-wide grep can surface fixture text, so a future reviewer
  prompt could say to exclude `evals/` from searches.

## Run 2

**Trigger:** new scenario FR-05, a subtler planted defect added so the eval can
discriminate (commit 84f889d).

**Rotation:** none. FR-05 is new; no existing scenario was changed.

**Scenarios run:** FR-05 only, three times. Each run used a fresh
`general-purpose` agent, never a fork, and all three ran in parallel. Each got
FR-05's Spec and Diff, the path `docs/NON_NEGOTIABLES.md`, and Stage 4's
reviewer instruction copied verbatim from `.claude/skills/feature/SKILL.md`,
plus HOW_TO_RUN.md step 2's extra line (don't read `evals/`, don't start the
dev server). No reviewer was told that a defect was planted.

**Diff context check before running:** the quoted code matches
`src/components/SearchDialog.tsx`: `const results = searchContent(query);` is
line 26, and `})}` / `</ul>` / the footer `<div>` are lines 136–138.
`searchContent`'s default `limit = 8` is at `src/lib/search.ts:72`, so the
planted defect is real. Only hunk metadata is off (noted, not fixed): the
second hunk shows 3 context lines, so `@@ -136,2 +138,7` should be
`@@ -136,3 +138,8`.

| ID    | Run | Finding summary                                                                                                                                                                                                                                                                                                                              | Rank of planted finding | Grade | Notes                                                                                          |
| ----- | --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------- | ----- | ---------------------------------------------------------------------------------------------- |
| FR-05 | 1   | Critical: "the line can never show, so criterion (2) is not met": `searchContent` defaults `limit = 8` (`search.ts:72`), so `hidden` is always 0; fix is a larger limit plus `results.slice(0, SHOWN)`. High: no test covers the behaviour. Medium: count not in a live region. Low: "+1 more results" plural. Low: 8 defined in two places. | 1 of 5                  | PASS  | Named the exact fix from Expected, and warned that the fix without a slice breaks criterion 1. |
| FR-05 | 2   | Critical: "`hidden` is always 0, so the '+N more' line never renders", same root cause. High: the obvious fix (larger limit) breaks criterion 1 without a slice. Medium: count not announced to screen readers. Low: plural at N=1.                                                                                                          | 1 of 4                  | PASS  | Also noted a mocked `searchContent` in tests could hide the bug.                               |
| FR-05 | 3   | Critical: "the new line can never appear, so criterion (2) fails", same root cause; fix is `searchContent(query, Infinity)` plus a slice. High: no test covers the >8 case. Medium: count outside a live region. Low: plural, layout shift, tokens fine.                                                                                     | 1 of 4                  | PASS  | Checked that Fuse slices only when `limit > -1`, so `Infinity` returns every match.            |

**Disagreements between runs:** none on grades or rank. All three put the
planted defect first at Critical with the fix Expected names. Secondary
findings matched closely too: all three raised the missing live region
(Medium) and the "+1 more results" plural (Low). Runs 1 and 3 ranked the
missing test High; run 2 instead ranked the slice-free fix trap High.

### Notes

- **FR-05 does not discriminate yet.** It was added because FR-01..03's planted
  defects were always the top finding. FR-05's was too, in 3 of 3 runs, each
  time found by reading `searchContent`'s signature one hop away. The ceiling
  effect persists. A harder variant would need the cap further from the call
  site (for example, set in a config or a caller two hops away), or a defect
  that only shows under a runtime condition.
- **No reviewer stopped at the missing test**, so the AMBIGUOUS case in
  Expected didn't occur.
- **Unplanted findings, all true of the diff:** the count is outside the
  dialog's persistent `role="status"` region, so screen readers aren't told
  (all three, Medium); "+1 more results" is ungrammatical (all three, Low).
  Neither is counted against the reviewers.
- **Scope.** No reviewer started the dev server or edited the working tree, and
  each said it read nothing under `evals/`.
