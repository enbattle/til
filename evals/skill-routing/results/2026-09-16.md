# Skill-routing eval — 2026-09-16

Run by: self (Claude, on request, as part of the change that added the
`content-review-eval` skill and the `content-review` eval category)
Trigger: `docs/specs/content-review-eval.md` — adding a new skill
required a new routing scenario (`SR-12`) per `skill-routing-eval`'s own
Stage 3, plus a regression check on the two adjacent existing scenarios
most likely to collide with its scope (`SR-10` for `skill-routing-eval`
itself, `SR-11` for `content-audit`). This run also fixed a pre-existing
staleness bug found along the way: both `skill-routing-eval/SKILL.md`'s
Stage 1 instruction and `skill-routing/HOW_TO_RUN.md`'s own summary of it
enumerated only `/feature`, `add-topic`, `docs-audit`, `content-audit`
as routing options — `skill-routing-eval` itself was missing from that
list even though `SR-10` already tests routing to it. Both were updated
to include `skill-routing-eval` and the new `content-review-eval`.

Each scenario below was run as a genuinely fresh `general-purpose`
subagent with no context beyond the scenario prompt itself (never a
`fork`), instructed to state a routing decision among `/feature`,
`add-topic`, `docs-audit`, `content-audit`, `skill-routing-eval`,
`content-review-eval`, or direct, without implementing anything.

| ID    | Routing decision      | Reasoning (1 line)                                                                                                                                                                                                     | Grade |
| ----- | --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- |
| SR-12 | `content-review-eval` | The task is exactly this skill's stated trigger — checking whether `add-topic`'s Stage 3 review still catches a planted content-quality violation after its wording changed, distinct from routing-correctness checks. | PASS  |
| SR-10 | `skill-routing-eval`  | Matches `skill-routing-eval`'s own trigger verbatim ("run the skill-routing eval"); no other skill covers executing that eval and it isn't a direct content/code edit.                                                 | PASS  |
| SR-11 | `content-audit`       | The three named checks (AI-sounding phrasing, over-explained analogies, questionable technical claims) plus "sweep across everything already published" is exactly `content-audit`'s stated scope.                     | PASS  |

**3/3 PASS.** Adding `content-review-eval` did not create routing
ambiguity with either of its most likely collision neighbors —
`skill-routing-eval` (a different failure surface: routing-correctness,
not review-efficacy) and `content-audit` (a different scope: a
corpus-wide prose sweep, not a check of the review process itself).

## Notes

- No further scenarios were re-run (`SR-01` through `SR-09`) since none
  of their Expected answers depend on anything this change touched —
  they don't reference `content-review-eval`'s scope or the Stage 1
  instruction text that was fixed.
- The Stage 1 instruction staleness (missing `skill-routing-eval` from
  its own enumerated options) was a real, pre-existing gap, not
  introduced by this change — worth noting since it means `SR-10` had
  been passing correctly in past runs despite the instruction text
  technically not listing that answer as an option, which only worked
  because the instruction also tells the agent to "explore the codebase
  as needed" rather than pick strictly from the named list.
