# Skill-routing eval — 2026-09-23

Run by: self (Claude, on request, on branch `chore/process-hardening`)
Trigger: this branch edited `.claude/skills/feature/SKILL.md` (Stages 1-6:
test-lock snapshot/verify/clear, the Stage 2 re-run gate, `review:diff`,
non-negotiables, the pipeline-log row), `.claude/skills/add-topic/SKILL.md`
(non-negotiables in Stage 3, a pipeline-log row in Stage 4), `CLAUDE.md` (the
escaped-defect line, new npm scripts) and `docs/SDLC.md`; added a new skill,
`feature-review-eval` (scenario `SR-14` was added for it); and edited
`skill-routing-eval`'s canonical routing list. Scenarios run: `SR-01` and
`SR-07` (expect `/feature`), `SR-05` (ambiguous by design), `SR-03` (must stay
direct), `SR-12` and `SR-13` (eval-skill neighbors of the new skill), `SR-14`
(new) and `SR-02` (the only scenario whose Expected is `add-topic`).

Each scenario was run as a genuinely fresh `general-purpose` agent (never a
`fork`), given only the prompt verbatim plus Stage 1's instruction, and told
not to read `evals/`.

| ID    | Routing decision                | Reasoning (1 line)                                                                                                                                 | Grade                 |
| ----- | ------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------- |
| SR-01 | `/feature`                      | Paginating search is a UI behavior change with design choices (page size, controls, keyboard, reset on query change) needing tests and UI review.  | PASS                  |
| SR-02 | `add-topic`                     | A new topic file in an existing section with no app code is exactly `add-topic`'s scope; would check `vector-search.md` to avoid overlap.          | PASS                  |
| SR-03 | Direct                          | CLAUDE.md names a typo as the example of the "small, unambiguous" carve-out; no skill covers a one-word README fix.                                | PASS                  |
| SR-05 | `/feature`                      | Read `SearchDialog.tsx`, found its Escape handler just calls `onClose`, suspected `App.tsx` state or the async body load, so called it nontrivial. | AMBIGUOUS (near-miss) |
| SR-07 | `/feature`                      | New app-code feature: a reading-time calculation plus UI changes on cards and the topic page.                                                      | PASS                  |
| SR-12 | `content-review-eval`           | Checking that `add-topic`'s Stage 3 review still catches a planted violation is this skill; the user ruled out a routing check.                    | PASS                  |
| SR-13 | `system-design-navigation-eval` | A question added and a summary reworded, then "does a symptom still reach the right question" is exactly this eval's trigger.                      | PASS                  |
| SR-14 | `feature-review-eval`           | Stage 4's reviewer instruction changed; this skill plants a defect in a diff and checks a fresh reviewer catches it, with a clean control.         | PASS                  |

**7 PASS, 1 AMBIGUOUS (near-miss).** No regression from the branch's edits.

## Notes

- **SR-14 routed to the new skill first time**, and the agent explicitly ruled
  out the three named failure routes' neighbors: `/feature` (nothing is being
  built), `content-review-eval` (that tests `add-topic`'s review) and the
  built-in `code-review` (reviews a real diff rather than testing the
  reviewer). `SR-12` still went to `content-review-eval`, so the new skill
  didn't pull its closest neighbor away.
- **SR-05 flipped direction but not grade.** The two 2026-09-21 runs went
  direct; this one went `/feature`. Both are acceptable routes, but this
  agent read one file, didn't find the cause, and chose `/feature` because
  the cause was unclear, which is still committing to a process before
  finding the cause. It investigated less than the 2026-09-14 PASS (six
  files, named interacting causes). Three near-misses in a row across two
  directions points at the scenario's grading or the eval prompt's forced
  early call, not at this branch's edits; the recommendation from 2026-09-21
  (accept "investigate, then decide" or revise Stage 0's guidance on bugs of
  unknown size) still stands.
- `SR-01`, `SR-03`, `SR-07` and `SR-02` answered the same as every prior run;
  none of the agents mentioned the new test-lock, `review:diff` or
  pipeline-log text, which is expected since none of it changes when a skill
  applies.
