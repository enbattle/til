# Skill-routing eval — 2026-09-21

Run by: self (Claude, on request, after the `/feature` run that made topic
bodies load lazily)
Trigger: `.claude/skills/feature/SKILL.md` (new Stage 6 retrospective, spec
"As built" and guard-script rules in Stages 3 and 4, a viewport method in the
reviewer's instruction, out-of-scope findings in Stage 5), `docs/SDLC.md`
(stage 6), `docs/DEFERRED_PRACTICES.md` ("Adding an entry") and one phrase in
`CLAUDE.md`'s pipeline summary. No skill `description` changed, so this checks
the scenarios whose routing turns on `/feature`: `SR-01` and `SR-07` (expect
`/feature`), `SR-04` and `SR-05` (ambiguous by design), and `SR-03`, `SR-06`
and `SR-08` (must stay direct). The `add-topic`, audit and eval-skill
scenarios weren't run: none of the files they route on changed.

Each scenario was run as a genuinely fresh `general-purpose` agent (never a
`fork`), given only the prompt verbatim and told not to read `evals/`.

| ID    | Routing decision                                         | Reasoning (1 line)                                                                                                                           | Grade                 |
| ----- | -------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- | --------------------- |
| SR-01 | `/feature`                                               | New search-dialog behavior with keyboard and focus choices, touching app code and tests.                                                     | PASS                  |
| SR-03 | Direct                                                   | Decided from CLAUDE.md alone that a one-word README typo is the "small, unambiguous" carve-out.                                              | PASS                  |
| SR-04 | Direct, via CLAUDE.md's "Adding a new section" steps     | `add-topic` excludes new sections; a folder, a registry entry and one topic is the documented lightweight path (accepted route).             | PASS (accepted route) |
| SR-05 | Direct, with `/feature` if the cause spans several files | Read four files, judged it "probably localized" to Escape handling, and said it would reconsider if the root cause turned out to be broader. | AMBIGUOUS (near-miss) |
| SR-06 | Direct                                                   | Editing existing prose is outside `add-topic`'s scope and is a "small, unambiguous" fix.                                                     | PASS                  |
| SR-07 | `/feature`                                               | New multi-file UI behavior; noted that `Topic` no longer carries a body, so a word count needs a design decision.                            | PASS                  |
| SR-08 | Direct, then `npm run verify`                            | Mechanical maintenance, not a feature; named `verify` as the check.                                                                          | PASS                  |

**6 PASS, 1 AMBIGUOUS (near-miss).** No scenario routed differently because of
the new text.

## Notes

- **SR-05 was a near-miss, not a regression.** The scenario fails a session
  that commits to a process before finding the cause. This agent explored
  (four tool calls) but named "direct" from a read of the code, not a
  reproduction, and hedged it. The 2026-09-14 run investigated further and
  landed on `/feature`. The text that produced this answer (Stage 0's
  carve-out and CLAUDE.md's "genuinely small, unambiguous") wasn't touched
  by this change, and the eval prompt itself forces a routing call up front,
  so treat it as noise unless it recurs.
- **SR-06's premise no longer matches the repo.** The agent found that
  `prompt-engineering.md` doesn't mention RLHF or 2024 (RLHF is only in
  `state-of-llms-september-2026.md`), and said it would check with the user.
  Routing was still right, but a scenario whose planted fact isn't there
  invites a clarifying question instead of a routing answer. Worth repointing
  the scenario at a fact that exists the next time this file is edited; not
  changed in this run.
- **SR-07's agent picked up the lazy-loading change unprompted** (`Topic` has
  no `body`, so a reading-time estimate needs a design decision), which is a
  small sign the docs describe the current code.

## Re-check after the independent read (same day)

An independent reader reviewed the process edits (see the commit message) and
its confirmed findings changed `SKILL.md` (Stages 3, 4, 4a, 5, 6 and the agent
count), `docs/SDLC.md`, `docs/DEFERRED_PRACTICES.md` and one clause in
`CLAUDE.md`. Re-ran the three scenarios most sensitive to that text, on the
final files.

| ID    | Routing decision                                        | Reasoning (1 line)                                                                                         | Grade                 |
| ----- | ------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- | --------------------- |
| SR-01 | `/feature`                                              | Nontrivial, user-facing app-code change with design choices; needs tests and UI review.                    | PASS                  |
| SR-03 | Direct                                                  | One-word typo is CLAUDE.md's "small, unambiguous" carve-out.                                               | PASS                  |
| SR-05 | Direct: reproduce, add a regression test, fix, `verify` | Read `SearchDialog.tsx`, judged it a single Escape/onClose effect; no mention of reconsidering `/feature`. | AMBIGUOUS (near-miss) |

**SR-05 has now gone direct in both runs today.** The first run hedged toward
`/feature`; this one didn't. Both planned to reproduce before fixing, which
counts for something, but neither investigated the cause before naming a
process, which is what the scenario says should fail. The 2026-09-14 run
investigated more deeply and chose `/feature`. Two things are worth weighing
before treating it as a problem with the docs: the eval prompt tells the agent
to decide routing without implementing and to answer in under 100 words, so it
is pushed to commit early, and nothing in the edited files touches the Stage 0
wording it cites. If it goes direct again on a later, unrelated run, revise
either the scenario's grading (accept "reproduce first, then decide" as
investigating) or Stage 0's guidance on bugs of unknown size.
