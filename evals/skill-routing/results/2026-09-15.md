# Skill-routing eval — 2026-09-15

Run by: self (Claude, on request, as part of the content-quality-review
change that added the `content-audit` skill)
Trigger: `docs/specs/content-quality-review.md` — extending the Writing
Standard, `add-topic`'s Stage 3 review, and adding a new `content-audit`
skill required a red/green routing check on the new `SR-11` scenario
(per that spec's Stage 2/3 red-green framing), plus a regression check
on the two adjacent existing scenarios most likely to collide with the
new skill's scope (`SR-02` for `add-topic`, `SR-09` for `docs-audit`).

## Red confirmation (prior stage, before `content-audit` existed)

`SR-11` was added to `scenarios.md` and run against a fresh routing
agent before any of this change's implementation files existed (no
`content-audit/SKILL.md`, no Writing Standard extension). That run
confirmed the scenario was genuinely red: with no `content-audit` skill
to route to, a fresh agent could not have picked it. This entry doesn't
restate that run's verbatim transcript (it wasn't logged to this file at
the time); it's recorded here as the red half of the red/green pair this
entry's green confirmation closes out, per
`docs/specs/content-quality-review.md`'s acceptance criteria.

## Green confirmation — after implementation

Each scenario below was run as a genuinely fresh `general-purpose`
subagent with no context beyond the scenario prompt itself (never a
`fork`), instructed to state a routing decision among `/feature`,
`add-topic`, `docs-audit`, `content-audit`, or direct, without
implementing anything.

| ID    | Routing decision | Reasoning (1 line)                                                                                                                                                                                                                                     | Grade |
| ----- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----- |
| SR-11 | `content-audit`  | A quality sweep of already-published topic prose (AI-sounding phrasing, over-explained analogies, questionable technical claims) rather than doc/process staleness, a new topic, or an app feature — matches `content-audit`'s stated purpose exactly. | PASS  |
| SR-02 | `add-topic`      | New topic markdown file for an existing section (`ai-and-ml`, no existing embeddings topic found) — exact scope match, not a new section and not app code.                                                                                             | PASS  |
| SR-09 | `docs-audit`     | Request matches `docs-audit`'s trigger verbatim — auditing `CLAUDE.md`/docs/evals/SKILL.md files for staleness after a batch of recent changes — not a single-topic or code-feature case.                                                              | PASS  |

**3/3 PASS.** `content-audit` resolved correctly on its own dedicated
scenario, and its addition did not create routing ambiguity for the two
adjacent scenarios most likely to collide with its scope
(`add-topic`'s single-new-topic case, `docs-audit`'s meta-documentation
case).

## Notes

- SR-11's fresh agent explicitly distinguished `content-audit` from both
  neighbors unprompted (didn't just name the skill, explained why it
  wasn't `docs-audit` or `add-topic`) — a good sign the new skill's
  frontmatter `description` is distinct enough for routing, not just
  technically correct by keyword overlap.
- No further scenarios were re-run (e.g. `SR-01`, `SR-03` through
  `SR-08`, `SR-10`) since none of their Expected answers depend on
  anything this change touched — they don't reference `add-topic`'s
  Stage 3 wording or `docs-audit`'s scope note, and the Writing Standard
  extension only affects prose-review depth, not routing.
