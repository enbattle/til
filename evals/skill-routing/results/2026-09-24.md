# Skill-routing eval — 2026-09-24

Run by: self (Claude, on request, on branch `chore/process-hardening`)
Trigger: since the 2026-09-23 run, commits `67b58aa` and `87635bc` edited
`.claude/skills/feature/SKILL.md`, the `add-topic`, `content-audit`,
`docs-audit` and `feature-review-eval` `SKILL.md` files,
`.claude/hooks/nudge-sdlc.js` and `CLAUDE.md`. Scenarios run: `SR-01` and
`SR-07` (expect `/feature`), `SR-05` (ambiguous by design), `SR-03` (must stay
direct), `SR-02` (`add-topic`), `SR-09` (`docs-audit`), `SR-11`
(`content-audit`), `SR-12` (`content-review-eval`, `add-topic`'s eval
neighbor) and `SR-14` (`feature-review-eval`). `SR-04`, `SR-06`, `SR-08`,
`SR-10` and `SR-13` were skipped: nothing their Expected answers depend on
changed.

Each scenario was run as a fresh `general-purpose` agent (never a `fork`),
given only the prompt verbatim plus Stage 1's instruction, and told not to read
`evals/`. All nine ran in parallel.

| ID    | Routing decision      | Reasoning (1 line)                                                                                                                                         | Grade                 |
| ----- | --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------- |
| SR-01 | `/feature`            | Search is capped by `searchContent(query, limit = 8)`; paging past it needs choices on page size, controls, keyboard and focus across lib and dialog.      | PASS                  |
| SR-02 | `add-topic`           | A new topic file in an existing section with no app code; would check the existing `vector-search.md` for overlap while drafting.                          | PASS                  |
| SR-03 | Direct                | CLAUDE.md names a typo as the example of a change to make directly; no skill covers a one-word README fix.                                                 | PASS                  |
| SR-05 | `/feature`            | Read `SearchDialog.tsx`, found Escape just calls `onClose` and the async load already guarded, so called the cause unknown and likely spanning `App.tsx`.  | AMBIGUOUS (near-miss) |
| SR-07 | `/feature`            | New multi-surface UI feature (cards and topic page) with design choices: words-per-minute rate, what text counts, rounding.                                | PASS                  |
| SR-09 | `docs-audit`          | Checking whether docs went stale after a batch of changes is exactly this skill; `content-audit` is topic prose quality, not meta-docs.                    | PASS                  |
| SR-11 | `content-audit`       | A sweep of every published topic for AI-sounding prose, over-explained analogies and wrong claims; not `add-topic` (new drafts) or `docs-audit`.           | PASS                  |
| SR-12 | `content-review-eval` | Tests whether `add-topic`'s Stage 3 review still catches a planted violation; the user explicitly ruled out a routing check.                               | PASS                  |
| SR-14 | `feature-review-eval` | Stage 4's reviewer instruction changed; this skill plants a defect in a diff and checks a clean control, which `content-review-eval` and `/feature` don't. | PASS                  |

**8 PASS, 1 AMBIGUOUS (near-miss).** No regression from `67b58aa` or
`87635bc`.

## Notes

- **SR-05 is a near-miss for the fourth run in a row** (two direct on
  2026-09-21, `/feature` on 2026-09-23 and again here). This agent read a bit
  more than yesterday's (five tool calls; it checked the async-load guard as
  well as the Escape handler) but still did not find the cause. It chose
  `/feature` _because_ the cause was unknown, which is committing to a process
  before finding the cause, the scenario's stated fail condition. It is not a
  FAIL only because it did investigate first and named a plausible next place
  to look. The pattern is stable now: the eval's forced "decide now, under 100
  words" framing pushes agents to pick a process for an unexplained bug. The
  2026-09-21 recommendation (accept "investigate, then decide" as an answer, or
  give Stage 0/CLAUDE.md explicit guidance for bugs of unknown size) still
  stands and has not been acted on.
- **SR-09 and SR-11 kept their neighbors apart.** Both agents named the other
  audit skill and why it didn't fit, so the `content-audit` and `docs-audit`
  description edits didn't blur the boundary. `SR-12` and `SR-14` also each
  ruled out the other eval skill by name.
- Everything else answered the same as every prior run. No agent mentioned the
  `nudge-sdlc.js` change, which is expected: the hook fires on file edits and
  doesn't affect which skill a request routes to.

# Skill-routing eval — 2026-09-24 (second run, after `84f889d`)

Run by: self (Claude, on request, on branch `chore/process-hardening`)
Trigger: commit `84f889d` added a triage-first rule for bugs of unknown size to
`.claude/skills/feature/SKILL.md` Stage 0, mentioned it in `CLAUDE.md`'s
carve-out, rewrote `SR-05`'s grading to that rule and rewrote `SR-06`'s prompt
to match the real topic text. Scenarios run: `SR-05` twice (the scenario the
change targets), `SR-06` (new prompt), and the scenarios that lean on the
`CLAUDE.md` carve-out or Stage 0: `SR-01`, `SR-03`, `SR-04`, `SR-07` and
`SR-08`. `SR-02` and `SR-09` to `SR-14` were skipped: nothing their Expected
answers depend on changed.

Each run was a fresh `general-purpose` agent (never a `fork`), given only the
prompt verbatim plus Stage 1's instruction, and told not to read `evals/`. All
eight ran in parallel.

| ID      | Routing decision                          | Reasoning (1 line)                                                                                                                          | Grade |
| ------- | ----------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ----- |
| SR-01   | `/feature`                                | New UI behavior in `SearchDialog` with open choices: numbered pages or "show more", keyboard navigation across pages, the full-text limit.  | PASS  |
| SR-03   | Direct                                    | `CLAUDE.md` names a typo as the example of a change to make directly; no skill covers it.                                                   | PASS  |
| SR-04   | `/feature` (direct 3-step as alternative) | `add-topic` excludes new sections; unsure book recommendations fit the Writing Standard, so wants spec and review.                          | PASS  |
| SR-05 A | Triage first, then route                  | Cites Stage 0 and `CLAUDE.md`; would reproduce and find the cause, then fix directly with a regression test if local, else `/feature`.      | PASS  |
| SR-05 B | Triage first, then route                  | Same rule cited; suspects a debounce/timer race in `SearchDialog.tsx`; direct fix plus failing-first test if local, `/feature` if it spans. | PASS  |
| SR-06   | Direct                                    | A few words added to one bullet of an existing topic is the small, unambiguous carve-out; not `add-topic` (new topics only).                | PASS  |
| SR-07   | `/feature`                                | Multi-surface UI (cards and topic page) with design choices, including where to compute it given topic bodies load as separate chunks.      | PASS  |
| SR-08   | Direct, then `npm run verify`             | A minor bump has nothing to spec; bump, reinstall, run verify; hand any breaking change that needs code to `/feature`.                      | PASS  |

**8 PASS.** No regression from `84f889d`.

## Notes

- **SR-05 passed on both runs, ending a four-run near-miss streak.** Both
  agents quoted the new rule ("a bug of unknown size is triaged first"), named
  both branches and what decides between them, and did not propose a fix. Both
  read the code (five tool calls each) but neither claimed to know the cause,
  which is the behavior the scenario wants. The 2026-09-21 recommendation is
  now acted on.
- **SR-06 with the rewritten prompt** routed direct and ruled out `add-topic`
  by name. It also said it would verify the 2017 origin (Christiano et al.)
  before publishing, per the Writing Standard, without being asked.
- **SR-04** chose `/feature` with the direct 3-step process as its named
  alternative; both are acceptable. Its reason (book recommendations may not
  fit the teaching-topic standard) is new but defensible.
- **SR-08** said nothing in `CLAUDE.md` or `docs/` covers dependency updates
  and did not mention the Dependabot auto-merge added in `84f889d`, which
  already handles minor updates. Not a grading issue, but a session asked this
  could point out that Dependabot makes the manual bump mostly redundant.
